function Ship(name, length, spaces, orientation) {
  this.name = name;
  this.length = length;
  this.spaces = spaces; //this is an array of spaces
  this.orientation = orientation;
  this.hits = 0
}

